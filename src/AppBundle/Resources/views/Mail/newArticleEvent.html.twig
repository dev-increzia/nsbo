{% extends 'AppBundle:Mail:layout.html.twig' %}
{% block body %}
    <h2>Bonjour,</h2>
    <p>
        L'évènement {{entity.title}}, associé à l'article {{article.title}},  a été créé par {% if article.type == "association" %}le groupe / association {% elseif article.type == "merchant" %} le commerce / partenaire {% endif %}{{account.name}}, veuillez contacter par mail <a alt="Envoyer un e-mail à l'auteur" title="Envoyer un e-mail à l'auteur" href="mailto:{{account.email}}">{{account.email}}</a> pour plus d'information.
    </p>
{% endblock %}